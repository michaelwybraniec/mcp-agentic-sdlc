{
  "name": "mcp-agentic-sdlc",
  "version": "0.1.0",
  "description": "Model Context Protocol tool: init_sdlc creates agentic-sdlc folder in the app directory.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp-agentic-sdlc-backlog": "dist/backlog/cli.js"
  },
  "scripts": {
    "build": "tsc && node scripts/bundle-kanban-runner.js && npm run copy-assets",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "copy-assets": "cp -r src/templates dist && cp -r src/recipes dist",
    "test": "node test/workflow.test.js && node test/backlog.test.js && node test/backlog-mcp.test.js && node test/base-user-md.test.js && node test/git-commits.test.js && node test/launch.test.js && node test/activity.test.js",
    "backlog:sync": "node dist/backlog/cli.js sync",
    "backlog:watch": "node dist/backlog/cli.js watch",
    "backlog:serve": "node dist/backlog/cli.js serve"
  },
  "keywords": [
    "mcp",
    "agentic",
    "sdlc",
    "tool"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "chokidar": "^4.0.3",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
